---
phase: 02-deploy-bridge-contract-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/codebase/DEPLOY_OPENCLAW_RUNTIME_CONTRACT.md
autonomous: true
requirements:
  - INTEG-02
  - OPS-01
  - OPS-02
user_setup: []
must_haves:
  truths:
    - Sealos yaml、hotfix refresh 脚本、runtime 实际覆盖文件列表被整理成同一份契约文档
    - `/xiaozhi/v1/`、`/xiaozhi/ota/`、`/openclaw/bridge/ws`、`/admin/openclaw/` 的入口映射被明确说明
    - hotfix overlay 漂移风险被拆成可执行检查点，而不是继续散落在聊天记录里
  artifacts:
    - .planning/codebase/DEPLOY_OPENCLAW_RUNTIME_CONTRACT.md
  key_links:
    - `deploy/xiaozhi-sealos.yaml` 与 `deploy/refresh-xiaozhi-hotfix.sh` 的文件清单要能逐项对应
    - 文档必须说明 `openclaw_hub` config、Nginx 路由和 hotfix cp 列表三者的关系
    - 文档必须给出一份后续 smoke / readiness checklist 雏形
---

<objective>
Audit the Sealos deployment manifest and hotfix refresh path, then turn them into a deploy/runtime source-of-truth document.

Purpose: Stop treating deploy overlay as an implementation detail and make the runtime contract inspectable before more OpenClaw features pile on top of it.
Output: A `DEPLOY_OPENCLAW_RUNTIME_CONTRACT.md` codebase document that maps config, routes, hotfix files, drift risks, and a first-pass readiness checklist.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-deploy-bridge-contract-hardening/02-CONTEXT.md
@.planning/codebase/INTEGRATIONS.md
@.planning/codebase/CONCERNS.md
@deploy/xiaozhi-sealos.yaml
@deploy/refresh-xiaozhi-hotfix.sh
@xiaozhi-esp32-server/main/xiaozhi-server/core/api/openclaw_admin_handler.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Map deploy config, network routes, and runtime ownership</name>
  <files>.planning/codebase/DEPLOY_OPENCLAW_RUNTIME_CONTRACT.md</files>
  <read_first>.planning/phases/02-deploy-bridge-contract-hardening/02-CONTEXT.md, deploy/xiaozhi-sealos.yaml, .planning/codebase/INTEGRATIONS.md</read_first>
  <action>Create `.planning/codebase/DEPLOY_OPENCLAW_RUNTIME_CONTRACT.md` as the source-of-truth document for the deploy/runtime side of OpenClaw integration. Document the `openclaw_hub` config block, Nginx route exposure, container startup overlay behavior, and which runtime paths are served by websocket vs HTTP traffic. Keep the writing grounded in the repo’s actual deploy manifest rather than product assumptions.</action>
  <verify>rg -n "OpenClaw Hub Config|Route Map|Runtime Ownership|Hotfix Overlay" .planning/codebase/DEPLOY_OPENCLAW_RUNTIME_CONTRACT.md</verify>
  <acceptance_criteria>
    - `.planning/codebase/DEPLOY_OPENCLAW_RUNTIME_CONTRACT.md` contains `## OpenClaw Hub Config`
    - `.planning/codebase/DEPLOY_OPENCLAW_RUNTIME_CONTRACT.md` contains `## Route Map`
    - `.planning/codebase/DEPLOY_OPENCLAW_RUNTIME_CONTRACT.md` contains `## Runtime Ownership`
    - `.planning/codebase/DEPLOY_OPENCLAW_RUNTIME_CONTRACT.md` explicitly references `/openclaw/bridge/ws` and `/admin/openclaw/`
  </acceptance_criteria>
  <done>The team can explain which runtime endpoints exist, who serves them, and which deploy file currently defines them.</done>
</task>

<task type="auto">
  <name>Task 2: Audit hotfix refresh parity and produce readiness checks</name>
  <files>.planning/codebase/DEPLOY_OPENCLAW_RUNTIME_CONTRACT.md</files>
  <read_first>.planning/codebase/DEPLOY_OPENCLAW_RUNTIME_CONTRACT.md, deploy/xiaozhi-sealos.yaml, deploy/refresh-xiaozhi-hotfix.sh</read_first>
  <action>Extend the same document with a file-by-file overlay audit: list the hotfix files copied by deployment startup, the files packed into the refresh script ConfigMap, and any drift or hidden dependency risks. Finish with a concise readiness checklist maintainers can use before claiming a deploy is aligned with local code.</action>
  <verify>rg -n "Overlay File Matrix|Drift Risks|Readiness Checklist" .planning/codebase/DEPLOY_OPENCLAW_RUNTIME_CONTRACT.md</verify>
  <acceptance_criteria>
    - `.planning/codebase/DEPLOY_OPENCLAW_RUNTIME_CONTRACT.md` contains `## Overlay File Matrix`
    - `.planning/codebase/DEPLOY_OPENCLAW_RUNTIME_CONTRACT.md` contains `## Drift Risks`
    - `.planning/codebase/DEPLOY_OPENCLAW_RUNTIME_CONTRACT.md` contains `## Readiness Checklist`
    - The document explicitly compares `xiaozhi-sealos.yaml` and `refresh-xiaozhi-hotfix.sh`
  </acceptance_criteria>
  <done>Maintainers have a concrete checklist for verifying deploy/runtime parity before debugging higher-level behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `test -f .planning/codebase/DEPLOY_OPENCLAW_RUNTIME_CONTRACT.md`
- [ ] `rg -n "OpenClaw Hub Config|Route Map|Overlay File Matrix|Readiness Checklist" .planning/codebase/DEPLOY_OPENCLAW_RUNTIME_CONTRACT.md`
- [ ] `rg -n "/openclaw/bridge/ws|/admin/openclaw|refresh-xiaozhi-hotfix.sh|hotfix" .planning/codebase/DEPLOY_OPENCLAW_RUNTIME_CONTRACT.md`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The deploy overlay ceases to be hidden tribal knowledge and becomes inspectable repo truth
- Phase 2 gains a concrete readiness baseline for later smoke verification
  </success_criteria>

<output>
After completion, create `.planning/phases/02-deploy-bridge-contract-hardening/02-01-SUMMARY.md`
</output>
