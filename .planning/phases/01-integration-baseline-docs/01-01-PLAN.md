---
phase: 01-integration-baseline-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/codebase/ARCHITECTURE.md
  - .planning/codebase/STRUCTURE.md
  - .planning/codebase/INTEGRATIONS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - INTEG-01
  - INTEG-03
  - PLAN-01
  - PLAN-02
  - DOC-01
  - DOC-02
user_setup: []
must_haves:
  truths:
    - 根仓文档清楚说明这是集成工作区而非单体仓库
    - `.planning` 文档与 roadmap / state 对当前 phase 的位置和下一步动作保持一致
    - 维护者可以从根仓文档追到 deploy overlay 与三子仓的职责边界
  artifacts:
    - .planning/PROJECT.md
    - .planning/REQUIREMENTS.md
    - .planning/codebase/ARCHITECTURE.md
    - .planning/codebase/STRUCTURE.md
    - .planning/codebase/INTEGRATIONS.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
  key_links:
    - `.planning/PROJECT.md` 和 `.planning/REQUIREMENTS.md` 明确 Phase 1 的范围与 out-of-scope
    - `.planning/codebase/*.md` 与 `deploy/` / `.gitmodules` 的描述一致
    - `.planning/ROADMAP.md` 与 `.planning/STATE.md` 指向下一步执行 01-01
---

<objective>
Formalize the brownfield bootstrap into durable root-level planning assets and align project state with executable Phase 1 work.

Purpose: Close the remaining `.planning` gaps before deeper contract validation starts in Phase 2.
Output: Refined root planning/codebase docs plus synchronized `ROADMAP.md` and `STATE.md`.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-integration-baseline-docs/01-CONTEXT.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/INTEGRATIONS.md
@.planning/codebase/CONCERNS.md
@.gitmodules
@deploy/xiaozhi-sealos.yaml
@deploy/refresh-xiaozhi-hotfix.sh
@pm.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tighten Phase 1 traceability across project and codebase docs</name>
  <files>.planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/codebase/ARCHITECTURE.md, .planning/codebase/STRUCTURE.md, .planning/codebase/INTEGRATIONS.md</files>
  <read_first>.planning/phases/01-integration-baseline-docs/01-CONTEXT.md, .planning/codebase/CONCERNS.md, .gitmodules, deploy/xiaozhi-sealos.yaml, deploy/refresh-xiaozhi-hotfix.sh, pm.json</read_first>
  <action>Refresh the root planning and architecture documents so they explicitly reflect the decisions captured in `01-CONTEXT.md`. Keep the root repo positioned as an integration workspace, make deploy overlay references concrete, and ensure every document points maintainers toward the same source-of-truth paths instead of hand-wavy summaries. Do not introduce submodule implementation work or new validation scope; document only what is already true in the repo and what Phase 2 will verify later.</action>
  <verify>rg -n "integration workspace|deploy overlay|Phase 2|pm.json|source of truth" .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/codebase/ARCHITECTURE.md .planning/codebase/STRUCTURE.md .planning/codebase/INTEGRATIONS.md</verify>
  <acceptance_criteria>
    - `.planning/PROJECT.md` contains `integration workspace` or `集成工作区` language that distinguishes the root repo from submodule implementation ownership
    - `.planning/REQUIREMENTS.md` still maps `INTEG-01`, `INTEG-03`, `PLAN-01`, `PLAN-02`, `DOC-01`, `DOC-02`, and `OPS-03` to Phase 1 without promoting Phase 2 verification work early
    - `.planning/codebase/ARCHITECTURE.md` contains `deployment overlay` or `deploy overlay` wording tied to root repo responsibilities
    - `.planning/codebase/STRUCTURE.md` mentions `README.md` as a maintainer landing page rather than a placeholder-only file
    - `.planning/codebase/INTEGRATIONS.md` keeps the immediate verification targets but frames them as Phase 2 follow-up, not completed validation
  </acceptance_criteria>
  <done>All five documents describe the same root-repo posture, point at the same source-of-truth files, and keep verification debt deferred to Phase 2.</done>
</task>

<task type="auto">
  <name>Task 2: Synchronize roadmap and state with the planned Phase 1 execution path</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>.planning/ROADMAP.md, .planning/STATE.md, .planning/phases/01-integration-baseline-docs/01-01-PLAN.md, .planning/phases/01-integration-baseline-docs/01-02-PLAN.md, pm.json</read_first>
  <action>Update roadmap progress and state tracking so GSD reports this phase as planned and ready to execute. Record that the bootstrap has been converted into standard phase artifacts, point the next action at `01-01-PLAN`, and mention that PM / Feishu sync baseline already exists through `pm.json` and created docs. Keep blockers intact and avoid claiming any plan execution is already complete.</action>
  <verify>rg -n "Planned|Plans ready for execution|01-01|pm.json|Phase 1" .planning/ROADMAP.md .planning/STATE.md</verify>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` progress row for Phase 1 contains `Planned`
    - `.planning/STATE.md` contains `Status: Plans ready for execution`
    - `.planning/STATE.md` contains `Current Plan: 1`
    - `.planning/STATE.md` contains a next-step note that names `01-01`
    - Neither file marks Phase 1 plans as completed before a matching `SUMMARY.md` exists
  </acceptance_criteria>
  <done>`gsd-progress` can read the planned state from root documents without inventing missing context.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "integration workspace|集成工作区" .planning/PROJECT.md .planning/codebase/ARCHITECTURE.md`
- [ ] `rg -n "Planned|Plans ready for execution|Current Plan: 1" .planning/ROADMAP.md .planning/STATE.md`
- [ ] `rg -n "Phase 2|Deferred|verification" .planning/codebase/INTEGRATIONS.md .planning/codebase/CONCERNS.md`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Root planning docs consistently describe the workspace as contract-driven integration governance
- GSD progress surfaces Phase 1 as planned and points execution to 01-01
  </success_criteria>

<output>
After completion, create `.planning/phases/01-integration-baseline-docs/01-01-SUMMARY.md`
</output>
