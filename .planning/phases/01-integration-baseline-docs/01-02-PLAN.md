---
phase: 01-integration-baseline-docs
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - README.md
autonomous: true
requirements:
  - INTEG-01
  - OPS-03
user_setup: []
must_haves:
  truths:
    - 根仓 README 能让维护者快速理解仓库存在意义、模块边界和查看顺序
    - README 把 `.planning`、`deploy/` 和三子仓连接成清晰的入口导航
    - README 不误导读者把根仓当作单体应用或统一构建入口
  artifacts:
    - README.md
  key_links:
    - README.md links readers to `.planning/PROJECT.md`, `.planning/ROADMAP.md`, and key codebase maps
    - README.md names `deploy/` as the runtime overlay boundary
    - README.md distinguishes root-repo duties from submodule implementation ownership
---

<objective>
Turn the root README into a maintainer-facing landing page that explains the workspace shape and where to continue work safely.

Purpose: Give non-GSD readers and future maintainers an immediate entry point without forcing them to reverse-engineer the repository.
Output: A root `README.md` with architecture overview, ownership boundaries, navigation, and next-step guidance.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@README.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-integration-baseline-docs/01-CONTEXT.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/INTEGRATIONS.md
@.planning/codebase/CONCERNS.md
@.gitmodules
@deploy/xiaozhi-sealos.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the README as a maintainer landing page</name>
  <files>README.md</files>
  <read_first>README.md, .planning/phases/01-integration-baseline-docs/01-CONTEXT.md, .planning/codebase/ARCHITECTURE.md, .planning/codebase/STRUCTURE.md, .planning/codebase/INTEGRATIONS.md, .gitmodules</read_first>
  <action>Replace the placeholder-only README with a maintainer-oriented introduction. Include: what this repo is, why the root repo exists, the role of each submodule, why `deploy/` is a first-class boundary, and the main system chain from device to server to OpenClaw agent. Keep the writing concise and operational. Do not paste large chunks from submodule READMEs; summarize and point to paths instead.</action>
  <verify>rg -n "^## " README.md && rg -n "xiaozhi-esp32|xiaozhi-esp32-server|openclaw-xiaozhi|deploy/" README.md</verify>
  <acceptance_criteria>
    - `README.md` contains a section heading `## What This Repo Is`
    - `README.md` contains a section heading `## Workspace Map`
    - `README.md` contains a section heading `## System Flow`
    - `README.md` mentions `xiaozhi-esp32`, `xiaozhi-esp32-server`, `openclaw-xiaozhi`, and `deploy/`
    - `README.md` explicitly states that the root repo is an integration workspace, not a merged monorepo
  </acceptance_criteria>
  <done>The README can orient a new maintainer in under a minute and points them to the correct source-of-truth paths.</done>
</task>

<task type="auto">
  <name>Task 2: Add maintainer navigation, safe-working rules, and next-step pointers</name>
  <files>README.md</files>
  <read_first>README.md, .planning/STATE.md, .planning/ROADMAP.md, .planning/codebase/CONCERNS.md, pm.json</read_first>
  <action>Extend the README with a small maintainer workflow section: where to start reading, what not to edit casually, how dirty submodules affect root-level changes, and which GSD / planning documents to consult next. Include links or path references to `.planning/PROJECT.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, and the relevant codebase maps. Make the “next up” section point maintainers toward Phase 2 after Phase 1 closeout, without pretending verification is already done.</action>
  <verify>rg -n "## Maintainer Workflow|## Safe Boundaries|## Next Up|\\.planning/PROJECT.md|\\.planning/ROADMAP.md|\\.planning/STATE.md|Phase 2" README.md</verify>
  <acceptance_criteria>
    - `README.md` contains `## Maintainer Workflow`
    - `README.md` contains `## Safe Boundaries`
    - `README.md` contains `## Next Up`
    - `README.md` references `.planning/PROJECT.md`, `.planning/ROADMAP.md`, and `.planning/STATE.md`
    - `README.md` warns about dirty submodules or root-level boundary safety before directing readers to future work
  </acceptance_criteria>
  <done>The README not only describes the repo, but also tells maintainers how to work in it safely and where the roadmap goes next.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "^## (What This Repo Is|Workspace Map|System Flow|Maintainer Workflow|Safe Boundaries|Next Up)" README.md`
- [ ] `rg -n "integration workspace|dirty submodules|Phase 2|deploy/" README.md`
- [ ] `test $(wc -l < README.md) -gt 40`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- README becomes a real maintainer entry point instead of a placeholder
- The repo boundary, deploy overlay role, and follow-up path are all explicit
  </success_criteria>

<output>
After completion, create `.planning/phases/01-integration-baseline-docs/01-02-SUMMARY.md`
</output>
